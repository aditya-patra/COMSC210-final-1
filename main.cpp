#include <iostream>
#include <fstream>

using namespace std;

int main() {
    ifstream file("210-final-1-FA25.txt");
    string start_airport;
    string end_airport;
    string flight;
    while(getline(file, flight)) {
        int start = 1;
    }
}